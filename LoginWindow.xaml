<Window x:Class="GroupeV.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TERMINAL VENDEUR - CONTRÔLE D'ACCÈS" 
        Height="700" Width="550"
        Background="Black"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Animations -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:1"/>
        </Storyboard>

        <Storyboard x:Key="SlideInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                           From="50" To="0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="1" To="1.05" Duration="0:0:1"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="1" To="1.05" Duration="0:0:1"/>
        </Storyboard>

        <Storyboard x:Key="GlowAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)"
                           From="20" To="40" Duration="0:0:1.5"/>
        </Storyboard>

        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0"/>
                <LinearDoubleKeyFrame Value="-10" KeyTime="0:0:0.05"/>
                <LinearDoubleKeyFrame Value="10" KeyTime="0:0:0.1"/>
                <LinearDoubleKeyFrame Value="-10" KeyTime="0:0:0.15"/>
                <LinearDoubleKeyFrame Value="10" KeyTime="0:0:0.2"/>
                <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0.25"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="LimeGreen"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="LimeGreen" BlurRadius="15" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00FF00"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="LimeGreen" BlurRadius="30" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="LimeGreen"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="LimeGreen"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoginTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="LimeGreen"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderBrush" Value="#00CC00"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CaretBrush" Value="LimeGreen"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="LimeGreen" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="LimeGreen"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="LimeGreen" BlurRadius="20" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoginPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="LimeGreen"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderBrush" Value="#00CC00"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CaretBrush" Value="LimeGreen"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="LimeGreen" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="LimeGreen"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="LimeGreen" BlurRadius="20" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="LimeGreen" BorderThickness="4">
        <Border.Effect>
            <DropShadowEffect Color="LimeGreen" BlurRadius="30" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#000000" Offset="0"/>
                    <GradientStop Color="#001a00" Offset="0.5"/>
                    <GradientStop Color="#000000" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Particules animées -->
            <Canvas x:Name="ParticlesCanvas" Grid.RowSpan="3" IsHitTestVisible="False">
                <!-- Particules ajoutées dynamiquement -->
            </Canvas>

            <!-- HEADER -->
            <Border Grid.Row="0" Background="#0A0A0A" Padding="20,20" x:Name="HeaderBorder">
                <StackPanel HorizontalAlignment="Center">
                    <!-- Logo animé -->
                    <Viewbox Width="80" Height="80" Margin="0,0,0,15">
                        <Grid RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform/>
                            </Grid.RenderTransform>
                            <Grid.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard Storyboard="{StaticResource PulseAnimation}"/>
                                </EventTrigger>
                            </Grid.Triggers>
                            <Ellipse Width="80" Height="80" Stroke="LimeGreen" StrokeThickness="3" Fill="Transparent"/>
                            <TextBlock Text="€" FontSize="50" FontWeight="Bold" Foreground="LimeGreen"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Viewbox>

                    <TextBlock x:Name="TitleText" Text="⚡ [TERMINAL VENDEUR] ⚡" 
                               FontFamily="Consolas"
                               FontSize="32" 
                               FontWeight="Bold"
                               Foreground="LimeGreen" 
                               HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="LimeGreen" BlurRadius="20" ShadowDepth="0"/>
                        </TextBlock.Effect>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard Storyboard="{StaticResource GlowAnimation}"/>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    
                    <TextBlock Text="🔐 SYSTÈME DE CONTRÔLE D'ACCÈS 🔐" 
                               FontFamily="Consolas"
                               FontSize="14" 
                               Foreground="#00CC00" 
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                    <TextBlock Text="═════════════════════════════════" 
                               FontFamily="Consolas"
                               FontSize="10" 
                               Foreground="#00CC00" 
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0" Opacity="0.7"/>
                </StackPanel>
            </Border>

            <!-- MAIN CONTENT -->
            <Border Grid.Row="1" Background="#0F0F0F" Padding="40,30" x:Name="ContentBorder">
                <StackPanel VerticalAlignment="Center">
                    <!-- Status Message -->
                    <Border Background="#0A0A0A" 
                            BorderBrush="#00CC00" 
                            BorderThickness="2" 
                            Padding="15" 
                            Margin="0,0,0,25"
                            Visibility="Collapsed"
                            x:Name="StatusBorder"
                            CornerRadius="5">
                        <Border.Effect>
                            <DropShadowEffect Color="LimeGreen" BlurRadius="15" ShadowDepth="0"/>
                        </Border.Effect>
                        <TextBlock x:Name="StatusTextBlock"
                                   Text=""
                                   FontFamily="Consolas" 
                                   FontSize="12" 
                                   Foreground="#00CC00"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   FontWeight="Bold"/>
                    </Border>

                    <!-- Email Field -->
                    <StackPanel x:Name="EmailPanel" RenderTransformOrigin="0.5,0.5">
                        <StackPanel.RenderTransform>
                            <TranslateTransform/>
                        </StackPanel.RenderTransform>
                        
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="📧 [ADRESSE EMAIL]" 
                                       FontFamily="Consolas" 
                                       FontSize="13" 
                                       Foreground="#00CC00"
                                       FontWeight="Bold"/>
                            <TextBlock Text="✦" 
                                       FontFamily="Consolas" 
                                       FontSize="13" 
                                       Foreground="Yellow"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                        <TextBox x:Name="EmailTextBox"
                                 Style="{StaticResource LoginTextBoxStyle}"
                                 Margin="0,0,0,20"/>
                    </StackPanel>

                    <!-- Password Field -->
                    <StackPanel x:Name="PasswordPanel" RenderTransformOrigin="0.5,0.5">
                        <StackPanel.RenderTransform>
                            <TranslateTransform/>
                        </StackPanel.RenderTransform>
                        
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="🔑 [MOT DE PASSE]" 
                                       FontFamily="Consolas" 
                                       FontSize="13" 
                                       Foreground="#00CC00"
                                       FontWeight="Bold"/>
                            <TextBlock Text="✦" 
                                       FontFamily="Consolas" 
                                       FontSize="13" 
                                       Foreground="Yellow"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                        <PasswordBox x:Name="PasswordBox"
                                     Style="{StaticResource LoginPasswordBoxStyle}"
                                     Margin="0,0,0,30"/>
                    </StackPanel>

                    <!-- Login Button -->
                    <Button x:Name="LoginButton" Content="🚀 CONNEXION" 
                            Style="{StaticResource LoginButtonStyle}"
                            Click="LoginButton_Click"
                            Height="50"
                            Margin="0,0,0,15"
                            RenderTransformOrigin="0.5,0.5">
                        <Button.RenderTransform>
                            <TranslateTransform/>
                        </Button.RenderTransform>
                    </Button>

                    <!-- Info -->
                    <Border Background="#0A0A0A" 
                            BorderBrush="#00AA00" 
                            BorderThickness="1" 
                            Padding="12" 
                            Margin="0,10,0,0"
                            CornerRadius="3">
                        <StackPanel>
                            <TextBlock Text="💡 INFORMATIONS" 
                                      FontFamily="Consolas" 
                                      FontSize="11" 
                                      Foreground="Yellow"
                                      FontWeight="Bold"
                                      Margin="0,0,0,5"/>
                            <TextBlock Text="• Utilisez vos identifiants de vendeur" 
                                      FontFamily="Consolas" 
                                      FontSize="10" 
                                      Foreground="#00AA00"
                                      TextWrapping="Wrap"/>
                            <TextBlock Text="• Contactez l'administrateur en cas de problème" 
                                      FontFamily="Consolas" 
                                      FontSize="10" 
                                      Foreground="#00AA00"
                                      TextWrapping="Wrap"
                                      Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#0A0A0A" Padding="15,12">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock Text="[BASE DE DONNÉES] vente_groupe" 
                                   FontFamily="Consolas" 
                                   FontSize="11" 
                                   Foreground="#00AA00"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="❌ FERMER" 
                                HorizontalAlignment="Right"
                                Padding="15,8"
                                Background="Transparent"
                                Foreground="Red"
                                BorderBrush="Red"
                                BorderThickness="2"
                                FontFamily="Consolas"
                                FontSize="11"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Click="CloseButton_Click"
                                ToolTip="Fermer l'application">
                            <Button.Effect>
                                <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                            </Button.Effect>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#330000"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Coins décoratifs animés -->
            <Canvas Grid.RowSpan="3" IsHitTestVisible="False">
                <TextBlock Text="╔" FontFamily="Consolas" FontSize="30" 
                          Foreground="LimeGreen" Canvas.Left="5" Canvas.Top="5" Opacity="0.5"/>
                <TextBlock Text="╗" FontFamily="Consolas" FontSize="30" 
                          Foreground="LimeGreen" Canvas.Right="5" Canvas.Top="5" Opacity="0.5"/>
                <TextBlock Text="╚" FontFamily="Consolas" FontSize="30" 
                          Foreground="LimeGreen" Canvas.Left="5" Canvas.Bottom="5" Opacity="0.5"/>
                <TextBlock Text="╝" FontFamily="Consolas" FontSize="30" 
                          Foreground="LimeGreen" Canvas.Right="5" Canvas.Bottom="5" Opacity="0.5"/>
            </Canvas>
        </Grid>
    </Border>
</Window>
