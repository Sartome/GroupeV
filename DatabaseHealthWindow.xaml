<Window x:Class="GroupeV.DatabaseHealthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Database Health Check" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource NeuBackgroundBrush}"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="💊  Vérification Santé BDD" 
                       FontFamily="{StaticResource NeuFontFamily}" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource NeuTextPrimaryBrush}"/>
            <TextBlock Text="Diagnostics système et validation de la connexion" 
                       FontFamily="{StaticResource NeuFontFamily}" 
                       FontSize="12" 
                       Foreground="{StaticResource NeuTextSecondaryBrush}" 
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Status Display -->
        <Border Grid.Row="1" Style="{StaticResource NeuCardInset}" Padding="16" CornerRadius="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="StatusPanel">
                    <TextBlock x:Name="StatusText" 
                               FontFamily="{StaticResource NeuMonoFont}" 
                               FontSize="12" 
                               Foreground="{StaticResource NeuTextPrimaryBrush}" 
                               TextWrapping="Wrap"
                               LineHeight="20"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            <Button Content="▶  Lancer le test" 
                    Click="RunCheck_Click"
                    Style="{StaticResource NeuButtonAccent}"
                    Padding="18,10" FontSize="13"
                    Margin="0,0,8,0"/>
            <Button Content="Fermer" 
                    Click="Close_Click"
                    Style="{StaticResource NeuButton}"
                    Padding="18,10" FontSize="13"/>
        </StackPanel>
    </Grid>
</Window>
